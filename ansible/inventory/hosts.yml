all:
  hosts:
    windows_vm:
      ansible_host: "{{ target_ip }}" 
  children:
    app_servers:
      hosts:
        windows_vm:
  vars:
    ansible_user: "Administrator"
    ansible_password: "SuaSenhaSeguraAqui"
    ansible_connection: "winrm"
    ansible_port: 5986
    ansible_winrm_server_cert_validation: "ignore"