network:
  version: 2
  ethernets:
    ens192:
      dhcp4: no
      addresses:
        - {{ static_ip }}/24
      routes:
        - to: default
          via: {{ gateway }}
      nameservers:
        addresses:
          - {{ dns_primary }}
          - {{ dns_secondary }}